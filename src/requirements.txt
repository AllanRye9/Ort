# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
gunicorn==23.0.0

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy==2.0.40
psycopg2-binary==2.9.10

# ── Pydantic / settings ────────────────────────────────────────────────────
pydantic==2.11.4
pydantic-settings==2.9.1
email-validator==2.2.0

# ── Auth / security ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# ── File upload ────────────────────────────────────────────────────────────
python-multipart==0.0.20

# ── HTTP client (used by currency conversion, AI, etc.) ───────────────────
httpx==0.28.1

# ── AWS S3 media uploads (optional – only used if S3_BUCKET is set) ────────
boto3==1.38.24

# ── OpenAI / AI endpoints ──────────────────────────────────────────────────
openai==1.84.0

# ── Environment helpers ────────────────────────────────────────────────────
python-dotenv==1.1.0

# ── Testing (not installed in prod image — kept here for CI) ───────────────
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.0
